server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book?useSSL=false
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: create-drop
#      TODO 测试用例完成之后，修改为update
    show-sql: true
